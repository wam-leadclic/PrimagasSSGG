trigger LeadPhone on Lead (before update) {
//	Lead [] auxLead = Trigger.new;
//	Lead[] books = Trigger.new;
  
 //Map<String, Lead> leadMap = new Map<String, Lead>();
 //   for (Lead lead : System.Trigger.new) {
    //
    //    if (opportunity.Codi__c != null) {
     //           leadAux.Phone = lead.Movil__c;
                //leadMap.put(lead.Email, lead);
    //    }
  //  }
}